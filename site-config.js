/* =====================================================================
   SITE CONFIG — بنية موحّدة للتوسع المستقبلي (بنية فقط، غير مطبّقة)
   =====================================================================
   هذا الملف هو «طبقة إعدادات» مركزية جاهزة للاستخدام مستقبلًا فقط.
   الغرض البنيوي الحالي: جمع كل ثوابت الهوية/السياق في مكان واحد ليصبح
   النقل إلى دعم أكثر من صفّ دراسي لاحقًا يسيرًا ودون إعادة كتابة نصوص.

   ⚠️ هام للغاية:
     - الملف حالياً **غير محمّل** في أي صفحة، ولا يُستخدم لاستبدال أي نص
       موجود (براند/فوتر/manifest كلها بدون تغيير).
     - لا يوجد أي دعم فعلي لصفوف دراسية متعددة.
     - كل القيم أدناه توثيقية/تجريبية فقط، وليست إلزامية لأي استخدام.

   النمط المتوقع عند التفعيل لاحقاً (ليس ضمن نطاق العمل الحالي):
     loadConfig()  ->   يعيد الكائن؛ وتقرأ منه الصفحات بدلاً من نصوصها الثابتة.
   حتى ذلك الحين لا يعتمد عليه أي كود.
   ===================================================================== */

const SITE_CONFIG = {
  /* --- الهوية الحالية (مرجعية، مطابقة للنصوص المعروضة) --- */
  siteName: 'Funny Science Lab',
  siteShort: 'Science Lab',
  brandText: '2nd Prep. Funny Science lab',

  siteAuthor: 'Dr. Wael Aboagiza',

  /* --- الصف الدراسي الحالي (معلومات سياقية فقط) --- */
  grade: {
    key: 'second-prep',
    label: 'الصف الثاني الإعدادي',
    enShort: '2nd Prep'
  },

  /* --- هيكل متوقّع محايد للصفوف (تجريبي — غير مطبّق وغير مدعوم) --- */
  scopeFuture: {
    multiGrade: false,
    note: 'الموقع الحالي مخصص لصفّ واحد فقط. هذا القسم توثيقي لمشروع مستقبلي محفوظ خارج نطاق التنفيذ، ويُترك هيكلياً حتى لا تحتاج البنية لتفكيك لاحقاً.'
  },

  /* --- مسارات نسبية إلزامية (تتوافق مع العمل تحت /Wael/ على GitHub Pages) --- */
  paths: {
    baseUrl: './',
    styleCss: 'style.css',
    experimentsCss: 'assets/css/experiments.css',
    experimentsData: 'experiments-data.js',
    lessonsData: 'lessons-data.js',
    videosData: 'videos-data.js',
    quizzesData: 'quizzes-data.js'
  }
};

/* eslint-disable no-unused-vars */
// (لا يوجد تصدير — الملف غير محمّل حالياً في أي صفحة؛ القيم متاحة فقط عند الحاجة مستقبلاً.)
